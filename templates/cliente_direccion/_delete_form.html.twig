<form method="post" action="{{ path('cliente_direccion_delete', {'id': cliente_direccion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cliente_direccion.id) }}">
    <button class="btn">Delete</button>
</form>