{% extends 'base.html.twig' %}

{% block title %}ClienteDireccion{% endblock %}

{% block body %}
    <h1>ClienteDireccion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cliente_direccion.id }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ cliente_direccion.direccion }}</td>
            </tr>
            <tr>
                <th>Ciudad</th>
                <td>{{ cliente_direccion.ciudad }}</td>
            </tr>
            <tr>
                <th>Provincia</th>
                <td>{{ cliente_direccion.provincia }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ cliente_direccion.cp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cliente_direccion_index') }}">back to list</a>

    <a href="{{ path('cliente_direccion_edit', {'id': cliente_direccion.id}) }}">edit</a>

    {{ include('cliente_direccion/_delete_form.html.twig') }}
{% endblock %}